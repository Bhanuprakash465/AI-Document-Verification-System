# AI Document Verification System - backend dependencies
# Verified working with Python 3.12 on Windows (CPU-only PyTorch build).

fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20
pydantic==2.13.4

# Image processing
opencv-python==4.11.0.86
numpy==2.5.1
pillow==12.3.0

# OCR (DocTR + PyTorch CPU)
python-doctr[torch]==0.11.0
torch==2.7.0
torchvision==0.22.0

# Database (optional persistence layer)
sqlalchemy==2.0.41

# Test client (used by tests/test_api.py)
httpx==0.27.2